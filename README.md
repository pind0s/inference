\[Work in progress\] \
LLM inference engine written in modern C++ with custom kernels for AVX512 and CUDA

- Currently only supports Qwen 3 models. (tested only on Qwen 3 0.6B) 
- Missing CUDA graph and any prefill optimizations
